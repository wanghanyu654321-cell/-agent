# Safety Knowledge Boundary

`approved/` may contain only entries explicitly reviewed and marked `status: approved`. This repository currently contains no production professional safety knowledge.

`fixtures/` is NON-PRODUCTION test data. Entries marked `synthetic_test_only` are rejected by production retrieval and may only be used with explicit test-mode construction.
