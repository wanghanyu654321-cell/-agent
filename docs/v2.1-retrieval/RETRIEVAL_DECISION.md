# V2.1 Retrieval Decision

`REAL_SOURCE_INPUT_REQUIRED`

Semantic retrieval is **not justified**: deterministic lexical retrieval has not been measured against a real approved corpus, so there is no evidence that embeddings, vector search, or a semantic reranker is necessary. V2.1 must resume from the preserved first-baseline procedure after authoritative input is supplied.
