# V2.2 Semantic Contract Review

## Historical mechanical result is preserved

The frozen V2.2 mechanical Gate remains **FAILED**. Its 24/24 calls, write-once manifest, fsync-backed journal, and derived report remain immutable. No retry or tuning occurred before the failure evidence was preserved.

## Why the true-insufficiency labels cannot adjudicate capability

All eight historical `TRUE_INSUFFICIENCY` evidence records included analyst-authored absence language in their model-visible `content`, for example “未给出具体小时数”, “未披露固定分值或权重”, “没有给出更正天数”, “未定义具体小时数”, “没有规定必须提交几份证据”, “没有给出固定处理天数”, “没有给出固定罚款金额”, and “没有给出统一的赔偿金额或计算公式”.

Those statements allow a candidate to directly support the truthful answer that the supplied rule does not specify the requested exact value. The frozen labels, calls, journal, and report are not changed, but the conclusion `7 FALSE SELECT = semantic capability failure` is not authorized.

**TRUE_INSUFFICIENCY CONTRACT INVALID FOR CAPABILITY ADJUDICATION.**

## Genuine V2.2 finding

**ORDER ROBUSTNESS FAILURE CONFIRMED.** The same query and same candidate identities produced `SELECT`/`ABSTAIN` disagreement after reversal in five cases. This observation is derived from model outcomes and does not rely on the disputed true-insufficiency gold interpretation.

Therefore V2.3.0 is **NOT RUNTIME ELIGIBLE**. No additional V2.3.0 real-model Gate is authorized, and Runtime Integration and release remain unauthorized.
