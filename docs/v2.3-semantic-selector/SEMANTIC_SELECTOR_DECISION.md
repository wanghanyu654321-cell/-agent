# V2.3 Semantic Selector Decision

The code-side contract and deterministic regressions are complete. The FIRST real run's empty extraction was not a semantic-quality observation: it used the OAuth-blind Pi compatibility completion path and received a failed `AssistantMessage` envelope. The adapter continues to map that error to typed `provider_error` rather than `invalid`.

The V2.3 evaluator and diagnostic now use public Coding Agent `ModelRuntime` OAuth-aware dispatch. One real control confirms configured Pi authentication, successful completion, a text block, non-zero output usage, and a response ID. **OAUTH-AWARE PI COMPLETION PATH VALIDATED.**

The first authorized OAuth-aware 44-call Gate process terminated before its final result report was written. The historical runner retained traces only in process memory and used a single direct `writeFileSync` only after all calls completed; it did **not** use an atomic report-write protocol. Its completion count, traces, metrics, and latency therefore cannot be reconstructed honestly; no rerun is authorized by that checkpoint. **SEMANTIC GATE INFRASTRUCTURE BLOCKED** and **SEMANTIC CAPABILITY NOT YET VALIDLY EVALUATED**. This does not justify a vector database, embeddings, a second Agent, or an LLM judge.

The one authorized durable Recovery Gate process at `df9cefdd1d10bb8764658798bbdbec766176e5ce` was blocked before `createDurableSemanticGateAttempt()` and before any semantic completion. Its local `git rev-parse HEAD` provenance command hit Git worktree ownership protection, so no manifest, journal, or final report was created and the real semantic call count is exactly `0`. The process is not retried under that authorization. **SEMANTIC GATE INFRASTRUCTURE BLOCKED** and **SEMANTIC CAPABILITY NOT YET VALIDLY EVALUATED** remain the controlling result. The hardened future lifecycle remains a distinct write-once manifest, fsync-backed traces, contiguous `1..44` reconstruction, and no-overwrite atomic derived-report publication; its journal is authoritative. See [GATE_EVIDENCE_DURABILITY.md](GATE_EVIDENCE_DURABILITY.md), [oauth-aware-semantic-gate-infrastructure-blocked.json](../../evals/selection/semantic/reports/oauth-aware-semantic-gate-infrastructure-blocked.json), and [OAUTH_AWARE_COMPLETION_PATH.md](OAUTH_AWARE_COMPLETION_PATH.md).

The subsequent authorized recovery-2 execution at `edb70e6c9d7e5b48defd5e42708e591d337bae4d` established the complete durable evidence set: its manifest was created before call #1, its journal has 44 unique contiguous records, and its write-once report is derived from that journal. There were no provider errors, timeouts, invalid outputs, wrong selections, or order-induced wrong selections. However, three valid abstentions leave `correctSelectionRate = 0.96`, below the frozen `0.98` requirement. Therefore **SEMANTIC SELECTOR OFFLINE GATE FAILED**. No tuning or rerun is authorized, and runtime integration and release remain prohibited.

The independent benchmark-contract audit classifies 41/50 cases as direct and sufficient, 9/50 as PARTIAL, and none as UNSUPPORTED. It corrects the prior shorthand: only `public-04/primary` and `public-28/primary` reduce the 50-case primary correct-selection rate; `public-04/reversed` is an order-robustness diagnostic. The benchmark is **PARTIALLY VALID** for a semantic sufficiency Gate. Recovery-2 remains historically **FAILED** and is not reinterpreted; see [BENCHMARK_CONTRACT_AUDIT.md](BENCHMARK_CONTRACT_AUDIT.md).

Audit V2 supersedes that V1 interpretation pending consistency repair: 37/50 are SUPPORTED and 13/50 are PARTIAL. It consistently treats reservation `怎么办` queries as PARTIAL when their gold only labels a non-reception scenario and gives no remedy, while `public-43` is SUPPORTED for its direct factual-status obligation. The benchmark remains **PARTIALLY VALID**. Recovery-2 remains frozen **FAILED**; see [BENCHMARK_CONTRACT_AUDIT_V2.md](BENCHMARK_CONTRACT_AUDIT_V2.md).
